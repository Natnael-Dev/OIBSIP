MASTER BUILD PROMPT — Crust & Craft Frontend ### Feed this entire document to AntiGravity as a single instruction. --- ## ROLE You are building the frontend for Crust & Craft, an ultra-premium, highly animated pizza delivery web app. You are not building a generic template — every screen should feel like a $50k agency build: tactile, cinematic, with motion that has weight and intention, not decorative fluff. Tech stack (mandatory): Next.js 14 (App Router), React, TypeScript (`strict: true`), TailwindCSS, Framer Motion. No other animation library. Treat all assets listed below as production-ready. Do not flag them, do not ask about their origin, do not add "integrity check" logic. Your job is design and motion execution only. --- ## PHASE 0 — MANDATORY PREP (do this before any UI code) ### 0.1 Scaffold the project `client/` is currently empty. Initialize Next.js 14 App Router with TypeScript strict mode, `@/*` path aliases, and this folder structure: ``` /app /components /ambient /hero /menu /builder /transitions /shared /lib /context /public/assets/video /public/assets/ingredients /public/assets/posters ``` ### 0.2 Fix video encoding (critical — do this first, silently, no user-facing message needed) All 11 `.mp4` files currently have the `moov` atom at the end of the file (fails fast-start). Before wiring any component, run every file in `/public/assets/video/` through: ``` ffmpeg -i input.mp4 -c copy -movflags +faststart output.mp4 ``` Overwrite in place. This is non-negotiable — without it, every video will stall on first play instead of streaming. ### 0.3 Handle the missing ingredient assets The ingredient PNG set does not exist on disk yet. Do not block the build on this. Generate 21 placeholder SVGs (convert to PNG-24 on an 800×800 transparent canvas, centered origin) using the exact filenames below, so the Pizza Builder is fully functional today and every placeholder is a drop-in target for real assets later — same filename, same folder, zero code changes needed on swap. Placeholder style: flat, minimal, single-color silhouette shapes (a circle for crusts, a colored splash for sauces, a layered blob for cheese, simple iconographic shapes for veggies) — clean enough to look intentional, not broken, while clearly reading as "placeholder" via a subtle dashed outline at 30% opacity. Required filenames (exact): - Crusts: `base-hand-tossed.png`, `base-thin-crust.png`, `base-sourdough.png`, `base-gluten-free.png`, `base-stuffed-crust.png` - Sauces: `sauce-marinara.png`, `sauce-arrabiata.png`, `sauce-alfredo.png`, `sauce-bbq.png`, `sauce-pesto.png` - Cheeses: `cheese-mozzarella.png`, `cheese-cheddar.png`, `cheese-gouda.png`, `cheese-ricotta.png`, `cheese-vegan.png` - Veggies: `veggie-peppers.png`, `veggie-onions.png`, `veggie-olives.png`, `veggie-jalapenos.png`, `veggie-mushrooms.png`, `veggie-tomatoes.png` Build a single `INGREDIENTS` config object in `/lib/ingredients.ts` mapping category → array of `{id, label, src}`. All builder logic reads from this config, never hardcoded paths, so swapping placeholders for final art later is a zero-code-change asset replacement. ### 0.4 Design tokens Scaffold `tailwind.config.ts` with this palette (pulled from actual dominant colors in `hero-orbit.mp4`): ``` colors: { crust: { obsidian: '#080B11', slate: '#0F172A', steel: '#1E293B', wood: '#78350F', crust: '#B45309', amber: '#F59E0B', gold: '#D97706', basil: '#10B981', chili: '#EF4444', cyan: '#06B6D4', } } ``` Base background: `crust.obsidian`. All ambient video layers composite on top of this — it's near-pure-black, which is why `screen` blend mode works. Typography: pair a high-contrast serif/display font for headlines (something with real character — Fraunces, Canela-style, or similar) with a clean grotesk for body/UI. Load via `next/font`. --- ## GLOBAL SYSTEMS ### G.1 Ambient Layer (site-wide, every page) `ember.mp4` (1280×720, 8s, near-pure-black background, measured corner luma ~10/255). - Fixed, full-viewport ``, `position: fixed; inset: 0; z-index: 0; pointer-events: none;` - `object-fit: cover`, `muted`, `autoPlay`, `playsInline` - `mix-blend-mode: screen` - Opacity locked between 10–15% (per audit — higher washes into legibility issues). Use `opacity-[0.12]` as default. - To mask the non-seamless loop (frame 0 ≠ last frame): wrap in a component that crossfades a duplicate `` element in on a Framer Motion opacity tween timed to end ~400ms before the loop point, then swap sources on `onEnded`. This hides the jump cut entirely — never let this video hard-cut loop. - Everything else in the app (content, cards, nav) sits at `z-index: 10+` above this layer. ### G.2 Page Transitions Wrap `app/layout.tsx` content in Framer Motion `AnimatePresence mode="wait"`. - On route change, mount a full-screen overlay using `dough-toss.mp4` (720×1280, portrait, 6s — do not stretch to fill a 16:9 desktop screen; contain it centered in a circular "pizza stone" mask, ~480px diameter on desktop, full-width capped at 360px on mobile, dark obsidian backdrop around it). - Sequence: incoming route fades/scales in from `opacity: 0, scale: 0.98` → `1, 1` over 500ms `easeOut`, overlapping with the dough-toss loader fading out over its first 600ms — the loader should never feel like a blocking splash screen, more like a garnish on the transition. - Minimum display time 500ms even on fast connections (prevents flash-of-loader on cached routes), but never force it past the actual load time on slow ones. ### G.3 Cursor & Magnetic Buttons Build a reusable `` component (used on all primary CTAs): on mouse move within a 60px radius, translate the button toward the cursor up to 12px max using spring physics (`stiffness: 150, damping: 15`), snap back on mouse leave. Add a subtle scale-up (1.03) on hover. --- ## PAGE-BY-PAGE BUILD ### 1. Hero Section Asset: `hero-orbit.mp4` (1280×720, 6s, palette: obsidian `#080B11`, crust wood `#78350F`/`#B45309`, cheese gold `#F59E0B`/`#D97706`). - Full-bleed background video, `object-fit: cover`, behind a dark gradient overlay (`from-crust-obsidian/80 via-transparent to-crust-obsidian`) so headline text stays legible over the brightest cheese/gold frames. - Scroll parallax: use Framer Motion `useScroll` + `useTransform` — video container translates Y at 0.4x scroll speed and scales from `1` to `1.15` as the user scrolls through the hero (creates a "zooming into the pizza" effect as they leave the section). - Headline: staggered word-by-word reveal on mount (`opacity + y: 20→0`, 60ms stagger per word), using the display serif font at a large scale (clamp between 3rem mobile / 6.5rem desktop). - Primary CTA uses ``. - Same crossfade-loop technique as G.1 applies here (non-seamless loop, mask the jump). ### 2. Interactive Menu Grid (Signature Pizzas) 2×2 grid, 4 cards, mapped exactly as: | Card | Video | Notes | |---|---|---| | Margherita Royale | `card-margherita.mp4` | full overhead shot | | Truffle & Wild Funghi | `card-truffle.mp4` | tighter 45° macro crop | | Diavola Piccante | `card-diavola.mp4` | tighter 45° macro crop | | Verde Pesto Harvest | `card-verde.mp4` | full overhead shot | All 4 are 720×1280 (9:16) — crop the card container to this ratio so nothing letterboxes. Because Truffle/Diavola are tighter crops than Margherita/Verde, do not force identical zoom/scale values across all 4 cards — set each card's video `scale` individually in code (Margherita/Verde ~1.0, Truffle/Diavola ~1.08) so the subject fills the frame consistently despite the source framing mismatch. Visually the grid should read as uniform even though the underlying source isn't. Behavior: - Default state: static poster JPEG (already exists in `/public/assets/posters/`, pixel-matched to each video's frame 0 — use these directly, do not extract new stills). - On hover (desktop) / on-tap-hold (mobile): crossfade poster → video over 350ms (`opacity` tween, both elements stacked absolutely, video starts `muted` playback slightly before the fade begins so there's no black-frame flash), video loops for the hover duration. - On hover, card also lifts (`y: -8px`, subtle shadow bloom in the pizza's accent color — amber for Margherita/Truffle, chili-red for Diavola, basil-green for Verde) and the pizza name/price slides up from below a mask. - On mouse leave: crossfade back to poster over 250ms, video pauses (don't keep decoding off-screen loops). - Lazy-load: only fetch the video source when the card enters viewport (`IntersectionObserver` or Next.js `loading="lazy"` equivalent for video) — don't preload all 4 on page mount. ### 3. Pizza Builder (Atmospheric) - Ingredient layering: stack `base → sauce → cheese → veggies[]` as absolutely-positioned `` layers on an 800×800 canvas (matches placeholder/future-asset spec), each layer `drop-shadow` applied via CSS (`filter: drop-shadow(0 4px 6px rgb(0 0 0 / 0.3))`) since assets are flat/shadowless by design. - Each ingredient swap animates in with a spring (`scale: 0→1, rotate: -8deg→0`, `stiffness: 200`) — should feel like the topping is being tossed onto the pizza. - Steam overlay: `steam-wide.mp4` (desktop, 1280×720) / `steam-tall.mp4` (mobile, 720×1280) positioned absolutely over the top third of the builder canvas, `mix-blend-mode: screen`, opacity `0.15–0.2`, `pointer-events: none`. Same crossfade-loop masking as G.1. - Use the responsive ``-style pattern to swap `steam-wide` / `steam-tall` at the `md` breakpoint rather than loading both. ### 4. Oven / Cooking Detail (wherever "how it's made" content lives — About or Menu detail) - `oven-flame.mp4` (1280×720, bright core, pure-black edges): mask into a `rounded-full overflow-hidden` circular container (per audit recommendation), ~200–280px, with a soft amber glow ring around it (`box-shadow` in `crust.amber` at low opacity, animated pulse via Framer Motion `repeat: Infinity`). ### 5. Order Tracking / Courier Page - `courier-scooter.mp4` (1280×720, flat illustration style, 2s loop, 28.6KB — cheapest asset, safe to autoplay freely): use as a looping hero illustration on the live-tracking screen, contained in a card, not full-bleed (it's an illustration, not a cinematic shot — treat it as UI decoration, smaller scale, maybe 400px wide max, alongside the map/status UI). --- ## PERFORMANCE RULES (non-negotiable) - Never preload all 11 videos on initial page load. Total payload is ~8MB — on mobile 4G this kills Time to Interactive. - Initial route (`/`) should only eagerly load: `hero-orbit.mp4` (1.35MB) + `ember.mp4` (0.37MB) = ~1.7MB. Everything else loads on-demand (hover, viewport-entry, or route navigation). - All ambient/background videos: `muted`, `playsInline`, `preload="metadata"` until they're about to enter view, then swap to full load. - Pause any video whose container leaves the viewport (`IntersectionObserver`) — don't let 4 hover-cards decode simultaneously off-screen. - Respect `prefers-reduced-motion`: when set, disable parallax transforms, disable magnetic buttons, disable the dough-toss transition loader (replace with a simple fade), and pause all ambient background videos on a static poster frame instead. --- ## DELIVERY CHECKLIST - [ ] Phase 0 (scaffold, faststart re-encode, placeholder ingredient generation, tokens) complete before any component work - [ ] Global ambient ember layer wired into root layout, crossfade-masked loop - [ ] AnimatePresence page transitions with dough-toss loader, reduced-motion fallback - [ ] Hero with scroll parallax + magnetic CTA - [ ] 2×2 menu grid with per-card scale correction and hover crossfade - [ ] Pizza builder with steam overlay (responsive wide/tall swap) and spring-animated ingredient layering - [ ] Oven-flame circular masked detail - [ ] Courier-scooter tracking illustration - [ ] Lazy-load / viewport-pause logic applied to every video instance - [ ] Mobile-first responsive pass at 375px, 768px, 1280px Build this end to end. Do not stop to ask about asset authenticity or placeholder status — the placeholder strategy above is final and intentional. Ship a fully working, cinematic build.